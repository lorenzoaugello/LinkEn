# LinkEn

The LinkEn Lemma Bank is the core of the LinkEn Knowledge Base. It consists of a large collection of English lemmas, serving as the backbone to achieve interoperability, by linking all those entries in lexical resources and tokens in corpora that point to the same lemma.

Data are coded both in a graph database (RDF triples - Turtle format).
